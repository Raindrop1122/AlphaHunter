name: Daily Alpha Hunter

on:
  schedule:
    # 每天北京时间早上 8:00 运行
    - cron: '0 0 * * *'
  # 允许你手动点击 "Run workflow" 按钮来测试
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # 必须确保你上传了 requirements.txt，否则这里会报错
        pip install -r requirements.txt

    - name: Run Scraper
      env:
        # 这里会自动读取你在 Settings -> Secrets 里存的 Key
        DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      run: |
        python scraper.py

    - name: Commit and Push changes
      run: |
        git config --global user.name "GitHub Action Bot"
        git config --global user.email "action@github.com"
        # 检查 papers.json 是否有变化
        git add papers.json
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Daily Update: New Papers Found" && git push)
