name: Daily Alpha Hunter Update

# 触发条件：
# 1. 每天 UTC 时间 20:00 (北京时间凌晨 4:00) 自动运行
# 2. 允许你在 GitHub 页面上手动点击 "Run workflow" 按钮触发
on:
  schedule:
    - cron: '0 20 * * *'
  workflow_dispatch:

jobs:
  update-papers:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 需要写权限来提交更新后的 papers.json

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install arxiv requests

      - name: Run Scraper
        env:
          # 这里会自动从你的 GitHub Secrets 读取你存的 DEEPSEEK_API_KEY
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: python scraper.py

      - name: Commit and push if changes
        run: |
          git config --global user.name 'Alpha Hunter Bot'
          git config --global user.email 'bot@alphahunter.io'
          git add papers.json
          # 如果 papers.json 有变化，则提交；否则不做任何事
          git commit -m "📈 Daily Alpha Update: $(date +'%Y-%m-%d')" || exit 0
          git push
