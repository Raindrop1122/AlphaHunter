name: Daily Alpha Hunter

on:
  schedule:
    # 每天北京时间早上 8:00 运行 (UTC时间 0:00)
    - cron: '0 0 * * *'
  # 允许手动点击按钮触发（方便测试）
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: Run Scraper
      env:
        # 把保险箱里的 Key 传给 Python 脚本
        DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      run: |
        python scraper.py

    - name: Commit and Push changes
      run: |
        git config --global user.name "GitHub Action Bot"
        git config --global user.email "action@github.com"
        git add papers.json
        # 如果文件有变化才提交，没变化就不提交
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Daily Update: New Papers Found" && git push)
